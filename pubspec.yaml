name: river_watch
description: River Water Gauge Management System
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  image_picker: ^1.1.2
  url_launcher: ^6.2.5

  intl: ^0.19.0
  provider: ^6.1.2
  shared_preferences: ^2.3.0
  google_fonts: ^6.2.1     # ← Added for Premium Fonts
  image: ^4.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/river_bg.jpeg